$(document).ready(function () {
    document.documentElement.style.fontSize = innerWidth/10+"px";   //设置页面字体大小是 宽度的1/10

    $("").bind("click",function () {
        if (True){

        }
        else{

        }
    })
});